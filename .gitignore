# Environment
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/

# Node
node_modules/
frontend/build/
frontend/.svelte-kit/
frontend/vite.config.ts.timestamp-*

# Playwright
frontend/test-results/
frontend/playwright-report/
# Ad-hoc QA screenshots. The ones the README uses are docs/screenshots/,
# which is tracked - this is the scratch directory a local run writes to.
frontend/screenshots/

# Local scratch kept deliberately out of the repo: probe scripts and
# one-off harnesses written while chasing a bug. Name them *.local.*
# rather than leaving them to be caught by a stray `git add`.
*.local.*
!.env.example

# Coverage
.coverage
htmlcov/

# Logs
*.log

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude Code (internal project spec + local settings)
CLAUDE.md
.claude/
